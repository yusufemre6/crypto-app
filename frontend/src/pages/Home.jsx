import React, { useContext } from "react";
import HeaderBar from "../components/Header";
import "../styles/global.css";

const Home = () => {
  return (
    <div>
      <HeaderBar></HeaderBar>
    </div>
  );
};

export default Home;
